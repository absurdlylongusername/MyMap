# Phase5 Acceptance (UI)

- AppHost shows `frontend` and `apiservice` healthy in Aspire Dashboard.
- Vite dev server proxies `/api/*` and `/auth/*` to API; no CORS errors.
- Routes exist: `/login`, `/map`, home.`
- Layout present: Top bar, sidebar, footer placeholders.
- Next: wire auth form, map canvas (Leaflet), bbox fetch, polygon tool, X-Data-Version surfacing.

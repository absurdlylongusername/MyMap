# Phase5 TODO — Step4

- BBox fetch + clustering.
- Category filter wiring.
- Polygon draw + POST query.
- Show `X-Data-Version` in footer and add Data info modal (lineage).
- Add a Sign out action (call Identity sign-out; then redirect to /login).
